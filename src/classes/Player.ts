class Player {
    name: string
    id: string
    card: string

    constructor(name: string, id: string, card: string) {
        this.name = name
        this.id = id
        this.card = card
    }
}

export default Player